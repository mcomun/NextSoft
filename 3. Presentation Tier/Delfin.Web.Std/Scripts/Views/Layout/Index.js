$(document).ready(function () {
   var cultura = Delfin.Web.Std.Global.Format.Language.toString().toLowerCase();
   //$.datepicker.setDefaults($.datepicker.regional[cultura]);
});

function setActive() {
}